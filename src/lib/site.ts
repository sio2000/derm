// ─────────────────────────────────────────────────────────────────────────
//  SINGLE SOURCE OF TRUTH FOR THE PRODUCTION DOMAIN
//
//  ⚠️  Set SITE_URL to the real site URL once the domain is live (no trailing
//      slash). It powers: canonical URLs, the sitemap (/sitemap.xml), robots
//      (/robots.txt), the PWA manifest AND — most importantly — makes the
//      Open Graph / Twitter share image (the Advanced Derma logo) resolve to an
//      ABSOLUTE url so it appears when the link is shared on Facebook,
//      WhatsApp, Viber, LinkedIn, Messenger, etc. Relative paths do NOT work
//      for social sharing. Change it here, in this one place only.
// ─────────────────────────────────────────────────────────────────────────
export const SITE_URL = 'https://advancedderma.gr';
export const SITE_NAME = 'Advanced Derma';
